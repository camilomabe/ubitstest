webdriver {
  driver = chrome
}

chrome.switches="""--start-maximized;--incognito;--warning-mode all"""

drivers {
  windows {
    webdriver.chrome.driver = "src/test/resources/webdriver/chromedriver.exe"
  }
}